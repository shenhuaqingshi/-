```markdown
# 电池正极AI助手 (Battery Cathode AI Assistant)

## 项目总览

本项目是一个用于预测锂离子电池正极材料性能的AI工具包。其核心是基于晶体图卷积神经网络（CGCNN）的多任务学习（MTL）模型，旨在同时预测材料的关键性能指标，如放电比容量、工作电压和循环寿命。项目设计注重可复现性和可扩展性，提供了从数据处理到模型预测的完整流程。

### 核心功能

*   **多任务学习 (MTL):** 同时预测多个性能指标，提高模型效率和泛化能力。
*   **图神经网络:** 利用CGCNN直接处理晶体的三维结构，捕捉原子间的空间关系。
*   **不确定性量化 (UQ):** 通过蒙特卡洛Dropout提供预测结果的置信区间。
*   **模块化设计:** 代码结构清晰，易于理解、修改和扩展。
*   **虚拟验证协议:** 提供了一套严格的虚拟验证方案，确保模型的可靠性和鲁棒性。

---

## 环境配置

### 1. 系统要求

*   **操作系统:** Windows, Linux, macOS
*   **Python版本:** 3.8 或更高

### 2. 安装依赖

建议使用虚拟环境（如 `conda` 或 `venv`）来管理依赖，以避免冲突。

#### 使用 Conda:

```bash
# 1. 创建新的conda环境
conda create -n battery_ai python=3.8

# 2. 激活环境
conda activate battery_ai

# 3. 安装 PyTorch (请根据您的CUDA版本选择命令，此处为CPU版本)
conda install pytorch torchvision torchaudio cpuonly -c pytorch

# 4. 安装其他依赖
pip install pandas scikit-learn numpy pyyaml
```

#### 使用 Pip:

```bash
# 1. 创建并激活虚拟环境 (可选但推荐)
python -m venv battery_env
source battery_env/bin/activate  # On Windows: battery_env\Scripts\activate

# 2. 升级 pip
pip install --upgrade pip

# 3. 安装 PyTorch (请根据您的CUDA版本选择命令，此处为CPU版本)
pip install torch torchvision torchaudio

# 4. 安装其他依赖
pip install pandas scikit-learn numpy pyyaml
```

### 3. 项目结构

克隆或下载项目后，您会看到以下主要文件和目录：

```auto
battery_cathode_ai/
├── README.md                 # 本文件，项目总览与环境配置
├── config.yaml               # 集中管理所有超参数与路径
├── models/
│   └── cgcnn_mtl.py          # 核心模型定义
├── data_loader.py            # 数据加载器，支持图模式
├── graph_builder.py          # CIF文件转晶体图的构建器
├── loss.py                   # 多任务损失函数
├── trainer.py                # 训练器与验证器
├── predict.py                # 预测脚本，含UQ接口
├── model_architecture_details.md # 模型架构详解
└── virtual_validation_protocol.md # 虚拟验证方案指南
```

### 4. 快速开始

1. **准备数据:** 确保您的 `cif_files/` 目录中有 `.cif` 结构文件，并准备好 `benchmark_set.json` 文件。
2. **调整配置:** 根据需要修改 `config.yaml` 中的路径和超参数。
3. **运行:** 您可以参考 `trainer.py` 和 `predict.py` 中的代码来启动训练或进行预测。

---

```auto
```